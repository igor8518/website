import React from 'react';
import { DashboardPage, DashboardPageTitle } from '../Common';

export const Distribution: React.FC = () => {
    return (
        <DashboardPage>
            <DashboardPageTitle>Distribution</DashboardPageTitle>
        </DashboardPage>
    );
};
